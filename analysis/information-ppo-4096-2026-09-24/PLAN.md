# PPO 扩大自我对局：追加4096场

用户授权扩大 PPO 训练对局数。2026-09-24登记，运行前固定。

- 从 information-pilot-2026-09-24/ppo/model.pt 第16轮权重继续，128批×32场，共新增4096场；累计4608场，末轮144。
- 源检查点未保存优化器和随机状态：本次恢复模型及已有历史对手快照，AdamW重新初始化。不能称作位级无缝恢复；学习率、batch64、3epochs、clip0.2、gamma1、lambda0.95、entropy0.01、KL0.02等不变。
- 训练种子为200000 + 全局轮号×100 + [0,31]，即201700–214431内的128个32种子块；避开先前训练／评测种子。
- 不加入新的棋力评测、不改变算法，不根据训练胜率挑版本；固定最后第144轮。现有离线dev仅作末尾诊断。
- 每批保存完整模型、优化器和随机状态；每16轮额外保留快照。完整比赛动作保留、结束后独立回放核验4096场和种子唯一性。
- 每10手输出比分及印章到SwanLab文字日志，每批输出训练指标与累计完成场数。训练使用actor采样，不调用1秒树搜索；后续搜索预算仍为每手1秒。
