# PPO 追加4096场

状态：已完成（第144轮）。新增4096场、323675个训练局面，耗时约14分23秒；422215次行动全部通过独立回放核验。配置及边界见 [PLAN.md](PLAN.md)，实际参数见 [config.json](config.json)。

从旧PPO第16轮模型权重继续，固定追加128批×32场。源检查点未包含优化器，因此AdamW状态重新初始化；新检查点保存模型、优化器与随机状态。

[实时SwanLab日志](https://swanlab.cn/@franzyu/jaipur-dmc/runs/vg7svbc0)，每10手输出筹码比分与印章，每批输出训练指标。`training.json` 为当前完成进度。

结束后脚本自动回放全部4096场；`verification.json` 出现且 verified=true 表示训练及回放完成。`swanlab-run.json` 记录运行状态。最终检查点为 `model-144.pt` / `model-144.json`，不会替换游戏内AI。本次训练完成后，按用户后续指示进行了独立棋力对照，见下方结果。

## 完成后的诊断

旧离线dev的公开价值MSE由第16轮0.8649降至0.8321；策略NLL由2.2647升至4.0126。训练策略熵由续训首批1.5635降至末批0.7174，行动分布更集中。NLL衡量对旧行为的拟合，不能直接等同棋力；这是训练结束时的诊断；后续独立评测已完成，见下方。

## 独立棋力对照已完成

新增512场对照、50934次行动回放通过。PPO144对PPO16为99:29；对normal为60:68，PPO16同种子为40:88；对DMC738为27:101。续训相对PPO16确有提升，但仍未达到替换旧模型门槛。[完整结果和配对统计](comparison/README.md)。

[训练量与算法设置的原因诊断](diagnosis/README.md)：检查点曲线仍整体上升，同时确认normal训练座位单边、历史池仅单快照等不足。
