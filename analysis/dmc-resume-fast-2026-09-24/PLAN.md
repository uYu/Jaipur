# DMC 增量训练方案（运行前记录）

从上轮最终 `model-658.pt` 连同优化器和随机状态继续，运行 **128 次更新**，每次 128 场完整自对局、4 个 epoch；其余网络、探索率 ε=0.1、行动空间和完整比赛胜负标签不变。起点选最终版本而非开发集所选 `model-612.pt`，以保持训练轨迹连续；新模型最终仍须和 `model-612.pt` 比较。

训练使用原有版本号派生的种子，不重复前 646 次更新。开发集改为 3,000,100,000–3,000,100,031 共 32 个配对种子；起点、每 16 次更新及末次均在该开发集评测，按胜负择优，同分保留较早版本。测试集另用未参加训练或选模的 3,500,000,000–3,500,000,031 共 32 个配对种子，与普通 AI 比较；另用 3,600,000,000–3,600,000,031 对原先所选 `model-612.pt` 比较，均交换座位、打完整比赛。先固定种子和配置，再查看任何评测结果。

优化仅增量缓存公开行动重放得到的已知对手牌，输入语义保持一致；不更改默认游戏 AI。训练批次张量、所有中间检查点和原始自对局留本地；保留选中、末次模型及可复核结果。若训练失败或被中断，应如实标明已完成版本，不把部分批次当成完成。
