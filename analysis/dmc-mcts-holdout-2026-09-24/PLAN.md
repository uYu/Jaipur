# DMC 根先验：新种子固定搜索次数复核（预登记）

本文件在运行评测前写入。使用未见于此前 MCTS 融合评测的种子 12001–12024，每个种子交换双方座位，合计 48 场完整比赛。候选为 `dmcMcts`（50% DMC 根先验），基线为 `guidedBehavior`。冻结 `analysis/dmc-10m-2026-09-24/model-612.pt`，不调温度或混合系数。

每方每次决策均为 8 棵树、每棵 256 次迭代，共 2,048 次；关闭时间截止和 DMC 超时回退。记录完整决策墙钟耗时。完成后独立回放所有比赛，检查合法行动、终局、种子及座位配对、胜负和每步搜索计数。

主指标为候选在 48 场中的胜数，同时按种子报告 2:0、1:1、0:2 的配对结果。报告以种子为独立单位的保守不确定性；这批种子只用于验证已有配置，不据此调参或宣称达到确定提升。若运行环境无法恢复，明确记录原因而不补造结果。
